language: objective-c
script: "env CC=gcc bash build.sh"
branches:
  only:
    - master
notifications:
  email:
    - andrew@andrewschaaf.com

# language: c
# script: "bash build.sh"
# compiler:
#   - clang
#   - gcc
